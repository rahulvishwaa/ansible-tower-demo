- name: Apply Secret
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: v1
      kind: Secret
      metadata:
        name: webapp-secret
        namespace: webapp
      type: Opaque
      data:
        DB_PASSWORD: "{{ db_password | b64encode }}"
        API_KEY: "{{ api_key | b64encode }}"


